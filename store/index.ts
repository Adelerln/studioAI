/**
 * Placeholder for global state management (Zustand, Jotai, or Redux).
 * Keeping the file lightweight prevents introducing unnecessary dependencies
 * while signalling where cross-feature state should live.
 */
export const studioStore = {
  /**
   * Example getter. Replace with real implementation when introducing state management.
   */
  getState() {
    return {};
  }
};
